spring:
  application:
    name: FinalProject

  datasource:
    url: jdbc:postgresql://localhost:5432/UserDB
    username: postgres
    password: sidhu

  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 9090